#include <iostream>
#include <set>
#include <vector>

int main()
{
    size_t n, k;
    size_t u, v, x;
    std::cin >> n >> k;
    std::vector<std::set<size_t>> sets{{}};
    while (--n) {
        std::cin >> u >> v >> x;
        if (x) {
            for (auto &s : sets) {
            }
        } else {
        
        }
    }
    return 0;
}
